export default function Hero() {
  return (
    <section className="hero">
      <h1>Make AI text sound <em>human.</em></h1>
      <p className="hero-sub">
        Paste your text, upload a document, or start typing — we'll handle the rest.
      </p>
    </section>
  );
}
